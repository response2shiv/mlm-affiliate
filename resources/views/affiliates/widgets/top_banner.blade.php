<div class="col-lg-12 my-3">
<!-- FIX IT
    <div class="header_image text-center">
        @if(!empty($url))
        <a href="{{ $url }}" target="_blank">
            <img src="{{ $img ?? 'http://via.placeholder.com/2560x582' }}" alt="{{ $alt }}">
        </a>
        @else
            <img src="{{ $img ?? 'http://via.placeholder.com/2560x582' }}" alt="{{ $alt }}">
        @endif
    </div>
-->   
</div>